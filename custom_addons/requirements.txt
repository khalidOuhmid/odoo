# Construction PLM - Python Dependencies
# Odoo.sh auto-installs this file from the repo root.
# Only packages NOT already bundled with Odoo 18.
#
# Already included in Odoo 18: Jinja2, PyPDF2, Pillow, openpyxl, lxml, Werkzeug

# PDF generation HTML→PDF (construction_contract)
# Requires system libs pre-installed on Odoo.sh: libpango, libcairo, libgdk-pixbuf
weasyprint>=60.0
