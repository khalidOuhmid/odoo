<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vue formulaire pour les documents -->
    <record id="view_blg_document_form" model="ir.ui.view">
        <field name="name">blg.document.form</field>
        <field name="model">blg.document</field>
        <field name="arch" type="xml">
            <form string="Document">
                <header>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Nom du document"/>
                        </h1>
                    </div>
                    
                    <group>
                        <group>
                            <field name="chantier_id" required="1"/>
                            <field name="document_type"/>
                            <field name="file" filename="filename"/>
                            <field name="filename" invisible="1"/>
                        </group>
                        <group>
                            <field name="expiry_date"/>
                            <field name="partner_id"/>
                        </group>
                    </group>

                    <group string="Description">
                        <field name="description" nolabel="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Vue liste pour les documents -->
    <record id="view_blg_document_tree" model="ir.ui.view">
        <field name="name">blg.document.tree</field>
        <field name="model">blg.document</field>
        <field name="arch" type="xml">
            <list string="Documents">
                <field name="name"/>
                <field name="chantier_id"/>
                <field name="document_type"/>
                <field name="expiry_date"/>
                <field name="state" widget="badge"/>
            </list>
        </field>
    </record>

    <!-- Action pour les documents -->
    <record id="action_blg_document" model="ir.actions.act_window">
        <field name="name">Documents</field>
        <field name="res_model">blg.document</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Créer le premier document
            </p>
            <p>
                Gérez tous les documents liés aux chantiers : contrats, attestations, plans...
            </p>
        </field>
    </record>
</odoo>
