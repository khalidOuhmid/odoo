<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="construction_contract.ContractEditor" owl="1">
        <div class="o_contract_editor_wrapper d-flex" style="height: 100vh; width: 100%; background: #f5f5f5;">

            <!-- ============ EDITABLE SIDEBAR (TASK-006) ============ -->
            <div class="o_contract_sidebar" style="width: 320px; background: linear-gradient(180deg, #1a1a2e 0%, #16213e 100%); color: #fff; flex-shrink: 0; display: flex; flex-direction: column; overflow: hidden;">

                <!-- Header -->
                <div style="padding: 16px 20px; border-bottom: 1px solid rgba(255,255,255,0.1); display: flex; align-items: center; gap: 10px;">
                    <i class="fa fa-file-text-o" style="font-size: 18px; color: #4ade80;"/>
                    <div>
                        <h6 style="margin: 0; font-weight: 600; font-size: 14px;">Contrat</h6>
                        <small style="color: rgba(255,255,255,0.5); font-size: 11px;" t-esc="state.contractName || 'Chargement...'"/>
                    </div>
                    <span class="badge ms-auto" t-att-class="{'bg-secondary': state.status === 'draft', 'bg-info': state.status === 'generated', 'bg-warning text-dark': state.status === 'sent', 'bg-success': state.status === 'signed'}" t-esc="state.statusLabel" style="font-size: 10px;"/>
                </div>

                <!-- Scrollable Fields Area -->
                <div style="flex: 1; overflow-y: auto; padding: 16px 20px;">

                    <!-- ===== SECTION: PARTIES ===== -->
                    <div class="sidebar-section mb-3">
                        <div class="sidebar-section-header" style="font-size: 10px; text-transform: uppercase; letter-spacing: 1px; color: rgba(255,255,255,0.4); margin-bottom: 10px; font-weight: 600;">
                            <i class="fa fa-users me-1"/>Parties
                        </div>

                        <div class="sidebar-field mb-3">
                            <label style="font-size: 11px; color: rgba(255,255,255,0.6); display: block; margin-bottom: 3px;">Sous-traitant</label>
                            <div style="font-size: 13px; font-weight: 500; padding: 6px 8px; background: rgba(255,255,255,0.08); border-radius: 6px;" t-esc="state.subcontractor || '—'"/>
                        </div>

                        <div class="sidebar-field mb-3">
                            <label style="font-size: 11px; color: rgba(255,255,255,0.6); display: block; margin-bottom: 3px;">Chantier</label>
                            <div style="font-size: 13px; font-weight: 500; padding: 6px 8px; background: rgba(255,255,255,0.08); border-radius: 6px;" t-esc="state.chantier || '—'"/>
                        </div>

                        <div class="sidebar-field mb-3">
                            <label style="font-size: 11px; color: rgba(255,255,255,0.6); display: block; margin-bottom: 3px;">Maître d'Ouvrage</label>
                            <input type="text" class="sidebar-input" t-att-value="state.masterName"
                                   t-on-change="(ev) => this.onFieldChange('master_name', ev.target.value)"
                                   placeholder="Nom du maître d'ouvrage"
                                   style="width: 100%; background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.15); border-radius: 6px; color: #fff; padding: 6px 8px; font-size: 13px;"/>
                        </div>

                        <div class="sidebar-field mb-3">
                            <label style="font-size: 11px; color: rgba(255,255,255,0.6); display: block; margin-bottom: 3px;">Adresse M.O.</label>
                            <input type="text" class="sidebar-input" t-att-value="state.masterAddress"
                                   t-on-change="(ev) => this.onFieldChange('master_address', ev.target.value)"
                                   placeholder="Adresse"
                                   style="width: 100%; background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.15); border-radius: 6px; color: #fff; padding: 6px 8px; font-size: 13px;"/>
                        </div>

                        <div class="sidebar-field mb-3">
                            <label style="font-size: 11px; color: rgba(255,255,255,0.6); display: block; margin-bottom: 3px;">Signataire BLG</label>
                            <input type="text" class="sidebar-input" t-att-value="state.signatoryContractor"
                                   t-on-change="(ev) => this.onFieldChange('signatory_contractor', ev.target.value)"
                                   placeholder="Direction BLG"
                                   style="width: 100%; background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.15); border-radius: 6px; color: #fff; padding: 6px 8px; font-size: 13px;"/>
                        </div>

                        <div class="sidebar-field mb-3">
                            <label style="font-size: 11px; color: rgba(255,255,255,0.6); display: block; margin-bottom: 3px;">Signataire Sous-traitant</label>
                            <input type="text" class="sidebar-input" t-att-value="state.signatorySubcontractor"
                                   t-on-change="(ev) => this.onFieldChange('signatory_subcontractor', ev.target.value)"
                                   placeholder="Nom du signataire"
                                   style="width: 100%; background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.15); border-radius: 6px; color: #fff; padding: 6px 8px; font-size: 13px;"/>
                        </div>
                    </div>

                    <!-- ===== SECTION: DATES ===== -->
                    <div class="sidebar-section mb-3">
                        <div class="sidebar-section-header" style="font-size: 10px; text-transform: uppercase; letter-spacing: 1px; color: rgba(255,255,255,0.4); margin-bottom: 10px; font-weight: 600;">
                            <i class="fa fa-calendar me-1"/>Dates
                        </div>

                        <div class="sidebar-field mb-3">
                            <label style="font-size: 11px; color: rgba(255,255,255,0.6); display: block; margin-bottom: 3px;">Début des travaux</label>
                            <input type="date" class="sidebar-input" t-att-value="state.startDate"
                                   t-on-change="(ev) => this.onFieldChange('start_date', ev.target.value)"
                                   style="width: 100%; background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.15); border-radius: 6px; color: #fff; padding: 6px 8px; font-size: 13px;"/>
                        </div>

                        <div class="sidebar-field mb-3">
                            <label style="font-size: 11px; color: rgba(255,255,255,0.6); display: block; margin-bottom: 3px;">Fin des travaux</label>
                            <input type="date" class="sidebar-input" t-att-value="state.endDate"
                                   t-on-change="(ev) => this.onFieldChange('end_date', ev.target.value)"
                                   style="width: 100%; background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.15); border-radius: 6px; color: #fff; padding: 6px 8px; font-size: 13px;"/>
                        </div>
                    </div>

                    <!-- ===== SECTION: MONTANTS ===== -->
                    <div class="sidebar-section mb-3">
                        <div class="sidebar-section-header" style="font-size: 10px; text-transform: uppercase; letter-spacing: 1px; color: rgba(255,255,255,0.4); margin-bottom: 10px; font-weight: 600;">
                            <i class="fa fa-eur me-1"/>Montants
                        </div>

                        <div class="sidebar-field mb-3">
                            <label style="font-size: 11px; color: rgba(255,255,255,0.6); display: block; margin-bottom: 3px;">Total HT</label>
                            <div style="font-size: 18px; font-weight: 700; color: #4ade80; padding: 6px 8px; background: rgba(74,222,128,0.08); border-radius: 6px;" t-esc="state.amount"/>
                        </div>

                        <div class="sidebar-field mb-3">
                            <label style="font-size: 11px; color: rgba(255,255,255,0.6); display: block; margin-bottom: 3px;">Retenue de garantie (%)</label>
                            <input type="number" class="sidebar-input" t-att-value="state.retentionRate"
                                   t-on-change="(ev) => this.onFieldChange('retention_rate', parseFloat(ev.target.value) || 0)"
                                   min="0" max="20" step="0.5"
                                   style="width: 100%; background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.15); border-radius: 6px; color: #fff; padding: 6px 8px; font-size: 13px;"/>
                        </div>

                        <div class="sidebar-field mb-3">
                            <label style="font-size: 11px; color: rgba(255,255,255,0.6); display: block; margin-bottom: 3px;">Durée GPA (mois)</label>
                            <input type="number" class="sidebar-input" t-att-value="state.gpaDuration"
                                   t-on-change="(ev) => this.onFieldChange('gpa_duration', parseInt(ev.target.value) || 12)"
                                   min="1" max="36"
                                   style="width: 100%; background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.15); border-radius: 6px; color: #fff; padding: 6px 8px; font-size: 13px;"/>
                        </div>
                    </div>

                    <!-- ===== SECTION: PÉNALITÉS ===== -->
                    <div class="sidebar-section mb-3">
                        <div class="sidebar-section-header" style="font-size: 10px; text-transform: uppercase; letter-spacing: 1px; color: rgba(255,255,255,0.4); margin-bottom: 10px; font-weight: 600;">
                            <i class="fa fa-gavel me-1"/>Pénalités (€)
                        </div>

                        <div class="d-flex gap-2 mb-2">
                            <div style="flex: 1;">
                                <label style="font-size: 10px; color: rgba(255,255,255,0.5); display: block; margin-bottom: 2px;">Retard/jour</label>
                                <input type="number" class="sidebar-input" t-att-value="state.penaltyRetardJour"
                                       t-on-change="(ev) => this.onFieldChange('penalty_retard_jour', parseFloat(ev.target.value) || 0)"
                                       min="0" step="10"
                                       style="width: 100%; background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.15); border-radius: 6px; color: #fff; padding: 5px 6px; font-size: 12px;"/>
                            </div>
                            <div style="flex: 1;">
                                <label style="font-size: 10px; color: rgba(255,255,255,0.5); display: block; margin-bottom: 2px;">Retard docs</label>
                                <input type="number" class="sidebar-input" t-att-value="state.penaltyDocsDelay"
                                       t-on-change="(ev) => this.onFieldChange('penalty_docs_delay', parseFloat(ev.target.value) || 0)"
                                       min="0" step="10"
                                       style="width: 100%; background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.15); border-radius: 6px; color: #fff; padding: 5px 6px; font-size: 12px;"/>
                            </div>
                        </div>

                        <div class="d-flex gap-2 mb-2">
                            <div style="flex: 1;">
                                <label style="font-size: 10px; color: rgba(255,255,255,0.5); display: block; margin-bottom: 2px;">Sécurité</label>
                                <input type="number" class="sidebar-input" t-att-value="state.penaltySafety"
                                       t-on-change="(ev) => this.onFieldChange('penalty_safety', parseFloat(ev.target.value) || 0)"
                                       min="0" step="10"
                                       style="width: 100%; background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.15); border-radius: 6px; color: #fff; padding: 5px 6px; font-size: 12px;"/>
                            </div>
                            <div style="flex: 1;">
                                <label style="font-size: 10px; color: rgba(255,255,255,0.5); display: block; margin-bottom: 2px;">Nettoyage</label>
                                <input type="number" class="sidebar-input" t-att-value="state.penaltyCleaning"
                                       t-on-change="(ev) => this.onFieldChange('penalty_cleaning', parseFloat(ev.target.value) || 0)"
                                       min="0" step="10"
                                       style="width: 100%; background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.15); border-radius: 6px; color: #fff; padding: 5px 6px; font-size: 12px;"/>
                            </div>
                        </div>

                        <div class="d-flex gap-2">
                            <div style="flex: 1;">
                                <label style="font-size: 10px; color: rgba(255,255,255,0.5); display: block; margin-bottom: 2px;">Justificatifs</label>
                                <input type="number" class="sidebar-input" t-att-value="state.penaltyJustificatifs"
                                       t-on-change="(ev) => this.onFieldChange('penalty_justificatifs', parseFloat(ev.target.value) || 0)"
                                       min="0" step="10"
                                       style="width: 100%; background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.15); border-radius: 6px; color: #fff; padding: 5px 6px; font-size: 12px;"/>
                            </div>
                            <div style="flex: 1;">
                                <label style="font-size: 10px; color: rgba(255,255,255,0.5); display: block; margin-bottom: 2px;">Prototypes</label>
                                <input type="number" class="sidebar-input" t-att-value="state.penaltyPrototypes"
                                       t-on-change="(ev) => this.onFieldChange('penalty_prototypes', parseFloat(ev.target.value) || 0)"
                                       min="0" step="10"
                                       style="width: 100%; background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.15); border-radius: 6px; color: #fff; padding: 5px 6px; font-size: 12px;"/>
                            </div>
                        </div>
                    </div>

                    <!-- ===== LOTS ===== -->
                    <div class="sidebar-section mb-3" t-if="state.lots and state.lots.length">
                        <div class="sidebar-section-header" style="font-size: 10px; text-transform: uppercase; letter-spacing: 1px; color: rgba(255,255,255,0.4); margin-bottom: 10px; font-weight: 600;">
                            <i class="fa fa-th-list me-1"/>Lots (<t t-esc="state.lots.length"/>)
                        </div>
                        <t t-foreach="state.lots" t-as="lot" t-key="lot.code">
                            <div style="padding: 6px 8px; background: rgba(255,255,255,0.05); border-radius: 6px; margin-bottom: 4px; font-size: 12px;">
                                <span style="font-weight: 600; color: #60a5fa;" t-esc="lot.code"/> — <span t-esc="lot.name"/>
                                <div style="font-size: 11px; color: rgba(255,255,255,0.4);" t-esc="lot.amount"/>
                            </div>
                        </t>
                    </div>

                </div>

                <!-- ===== ACTION BUTTONS ===== -->
                <div style="padding: 12px 20px; border-top: 1px solid rgba(255,255,255,0.1); flex-shrink: 0;">
                    <div class="d-flex gap-2 mb-2" t-if="state.isDirty">
                        <span style="font-size: 11px; color: #fbbf24;"><i class="fa fa-circle me-1" style="font-size: 6px;"/>Modifications non sauvegardées</span>
                    </div>
                    <button class="btn btn-success w-100 mb-2" t-on-click="onSaveSidebar" t-att-disabled="state.savingSidebar || !state.isDirty" style="font-size: 13px; font-weight: 600;">
                        <i class="fa fa-save me-2" t-if="!state.savingSidebar"/>
                        <i class="fa fa-spinner fa-spin me-2" t-if="state.savingSidebar"/>
                        Sauvegarder les champs
                    </button>
                    <button class="btn btn-primary w-100 mb-2" t-on-click="onGenerateAndSend" t-att-disabled="state.saving" style="font-size: 13px; font-weight: 600;">
                        <i class="fa fa-paper-plane me-2"/>Générer &amp; Envoyer
                    </button>
                    <button class="btn btn-outline-light w-100" t-on-click="onClose" style="font-size: 13px;">
                        <i class="fa fa-arrow-left me-2"/>Retour
                    </button>
                </div>
            </div>

            <!-- ============ GRAPEJS EDITOR ============ -->
            <div class="o_contract_editor_main" style="flex: 1; overflow: hidden;">
                <div t-ref="editor" style="height: 100%; width: 100%;"/>
            </div>
        </div>
    </t>
</templates>
